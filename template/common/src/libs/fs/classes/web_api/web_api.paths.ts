import type { DefaultPaths } from '../../interfaces/default_paths.interface';

export class WebApiPaths implements DefaultPaths {
  app = './';
  cache = './cache';
  data = './data';
  documents = 'documents';
  home = '/';
}
